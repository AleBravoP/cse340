<!-- Content Heading -->

<!-- Describe the content of the view with a <title> element -->
    <% if (title) { %>
        <h1><%= title %></h1>
        <% } else {
          res.redirect('/')
        } %>
    
    <!-- display a message in a view -->
    <%# messages() %>
    
    <!-- An EJS code block with an "unescaped" indicator — to allow HTML to be displayed. -->
    <%- item_view %>
    <%- comments_view %>

<!-- Form for adding new comments -->
<div id="comment">
  <form action="/inv/detail/<%= data.inv_id %>/add-comment" method="POST">
    <label for="comment">Add your comment:</label>
    <textarea name="comment" rows="4" cols="50" required></textarea>
    <br>
    <button type="submit">Submit Comment</button>
  </form>
</div>